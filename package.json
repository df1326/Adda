const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');

const app = express();

// Middleware
app.use(cors());
app.use(express.json());

// የኢንቫይሮመንት ፖርት እና የዳታቤዝ ዩአርኤል (Environment Variables)
const PORT = process.env.PORT || 5000;
const MONGO_URI = process.env.MONGO_URI;

// መሠረታዊ ራውት (Test Route)
app.get('/', (req, res) => {
    res.send('የቴክኖሎጂ ሽግግር ፕሮፋይል መሰብሰቢያ እና ማስተዳደሪያ ሲስተም በሰላም እየሰራ ነው!');
});

// የዳታቤዝ ግንኙነት እና ሰርቨሩን ማስጀመር
mongoose.set('strictQuery', true);

mongoose.connect(MONGO_URI)
    .then(() => {
        console.log('MongoDB successfully connected.');
        // ዳታቤዙ ከተገናኘ በኋላ ብቻ ሰርቨሩ ይነሳል
        app.listen(PORT, () => {
            console.log(`Server is running smoothly on port ${PORT}`);
        });
    })
    .catch((err) => {
        console.error('Database connection failed:', err.message);
        process.exit(1); // ሰርቨሩ ስህተት ሲያጋጥመው እንዲቆም እና ሎግ እንዲያሳይ
    });
